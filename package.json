{
  "name": "imax-schedule-monitor",
  "version": "1.0.0",
  "description": "IMAX 영화 스케줄 모니터링 시스템",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "check": "npm run build && npm run start check-once",
    "test:notification": "npm run build && npm run start test-notification",
    "test:scraping": "npm run build && npm run start test-scraping"
  },
  "dependencies": {
    "better-sqlite3": "^9.2.2",
    "dotenv": "^17.2.3",
    "node-cron": "^3.0.3",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "@types/node": "^20.10.0",
    "@types/node-cron": "^3.0.11",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
